Packages to be installed before 
csv- input command(pip install python-csv)

install the csv module using the input command in command prompt.

#execute the python script

#It will ask for input file name which is to be processed to a new file.

#and also give the output file name which will be the name of the new processed csv file.

note:the input file and python script should be in the same directory.